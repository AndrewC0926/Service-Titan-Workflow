# Line-card gap draft — item master reports

No item master file exists anywhere on this machine — checked the full home
directory, the mounted Windows `Downloads`/`Documents` folders, and every
`.csv`/`.xlsx` on disk for an item catalog or a "Preferred Vendor" column
(only three NetSuite exports exist, in `~/netsuite-exports/`: sales order
lines, quote lines, and the customer master used for
`docs/NETSUITE-DEDUPE-PROPOSAL.md` — none of them carry item-to-vendor data),
so reports (c), equipment dollars by Preferred Vendor, and card-line share
cannot be run; stopping here per instruction rather than guessing at a path
or a vendor mapping.
