# Copy to .env (or set in Render). Secrets only — everything else is config.yaml.
DATABASE_URL=postgresql://user:pass@localhost:5432/dmgscout
ANTHROPIC_API_KEY=sk-ant-...
DASHBOARD_PASSWORD=change-me
# Optional, only if digest.transport is set accordingly in config.yaml:
RESEND_API_KEY=
SMTP_PASSWORD=
